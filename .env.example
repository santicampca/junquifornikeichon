# Cadena de conexión a MongoDB Atlas (o cualquier Mongo compatible).
# La conseguís en Atlas > Database > Connect > Drivers, y le agregás el
# nombre de la base al final de la URL (antes de "?").
DATABASE_URL="mongodb+srv://usuario:password@cluster0.xxxxx.mongodb.net/torneosfc?retryWrites=true&w=majority"
